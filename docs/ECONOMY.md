# Economy

Definitions declare currencies. Credit and debit validate positive amounts; debit rejects insufficient balance unless `rules.economy.allowNegativeBalance` is enabled. Each operation persists a transaction record with a reason and timestamp. Use `transfer` for atomic movement between owners.
